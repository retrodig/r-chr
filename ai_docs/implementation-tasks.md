# 実装タスク

## 実装済み機能

### ファイル入出力
- [x] `.nes` ファイルの読み込み・保存（iNES フォーマット）
- [x] `.bin` ファイルの読み込み・保存（生 CHR バイナリ）
- [x] `.nes` / `.bin` のドラッグ & ドロップ対応
- [x] `.png` のインポート（CHR カラーインデックス変換）
- [x] `.png` のドラッグ & ドロップ対応
- [x] `.pal` ファイルの読み込み（マスターパレット差し替え）
- [x] `.dat` ファイルの読み込み・保存（DAT パレット）
- [x] 未保存変更の確認ダイアログ

### CHR 表示・操作
- [x] CHR 全体ビュー（128px 幅、16 タイル/行）
- [x] バンク切り替え（4KB 単位）
- [x] 選択ブロックサイズ切り替え（8 / 16 / 32 / 64 / 128 px）
- [x] アドレスジャンプ（16 進数入力）
- [x] テクスチャキャッシュ（texture_dirty フラグによる効率的な再描画）

### ドット編集
- [x] 8×8 ピクセルグリッドエディタ
- [x] 描画色選択（0〜3 の 4 色）
- [x] スポイトツール
- [x] アンドゥ（Cmd+Z / Ctrl+Z）

### パレット
- [x] DAT パレット（4 セット × 4 色）の表示・編集
- [x] NES マスターパレット 64 色ピッカー
- [x] デフォルトパレットへのリセット

### UI / UX
- [x] ダークモード / ライトモード切り替え
- [x] macOS ネイティブメニューバー（muda）
- [x] Windows / Linux egui メニューバー
- [x] 日本語フォント（Meiryo）対応
- [x] ウィンドウアイコン設定
- [x] macOS ウィンドウ枠ダークモード（NSAppearance）
- [x] 起動時デフォルト CHR データ表示（R-CHR ロゴ）

### ディレクトリ構成
- [x] `io/` `model/` `editor/` によるモジュール整理

---

## 今後の実装候補

### 高優先度

- [ ] **CHR → PNG エクスポート**
  - 現在の bank_texture（または全 CHR）を PNG ファイルとして保存
  - `image` クレートで `ColorImage` → PNG バイト列に変換

- [ ] **タイルコピー / ペースト**
  - 選択中タイルをクリップボードまたは内部バッファにコピー
  - 別のタイル位置にペースト

- [ ] **キーボードショートカットの拡張**
  - 矢印キーによるタイル移動の改善
  - フォーカスサイズ変更ショートカット

### 中優先度

- [ ] **NES ROM からのパレット変化解析**
  - PRG-ROM を逆アセンブルまたはバイトスキャンして PPU パレットレジスタ（$3F00〜）への書き込み命令を列挙
  - ゲーム中にどのパレットが使われているか一覧表示

- [ ] **スプライトプレビュー**
  - CHR の選択タイルを 8×8 または 8×16 スプライトとして拡大プレビュー
  - 複数タイルをまとめたキャラクタプレビュー

- [ ] **PAL ファイルの保存**
  - 現在の MasterPalette を `.pal` ファイルとしてエクスポート

- [ ] **macOS 以外のフォント対応**
  - Windows: `C:\Windows\Fonts\meiryo.ttc` など
  - Linux: システムフォントの検索

### 低優先度

- [ ] **複数バンク同時表示**
  - バンク一覧をサイドバーに並べてナビゲート

- [ ] **タイル検索 / フィルタ**
  - 空白タイル（全ピクセル 0）のハイライト・スキップ

- [ ] **ネームテーブルプレビュー**
  - 32×30 タイルのスクリーンレイアウトをシミュレート

- [ ] **グリッド線の表示設定**
  - バンクビューでタイル境界線の表示 / 非表示切り替え

- [ ] **ズーム機能**
  - バンクビューの拡大率変更

---

## 技術的負債・改善候補

- [ ] `setup_fonts()` のフォントパスをプラットフォーム別に自動検索（現在は macOS のみ Meiryo をハードコード）
- [ ] `PngImportDialog` のプレビュー再生成ロジックを `preview_dirty` フラグで最適化（実装済みだが動作確認要）
- [ ] `undo_stack` の上限サイズ設定（現在は無制限）